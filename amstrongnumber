l= int(input())
u=int(input ())

def armstrong (num):
    temp=num
    power=len(str(temp ))
    sum=0
    while (temp):
        digit=temp%10
        sum+=digit**power
        temp=temp//10
    if sum==num:
        return  True
    else:
        return  False


if l<=0 or u<=0:
    print ("wrong input")
else:
    for num in range(l,u+1):
        if(armstrong(num)):
            print (num)
